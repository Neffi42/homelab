---
# yaml-language-server: $schema=https://k8s-schemas.home-operations.com/kopiur.home-operations.com/snapshotpolicy_v1alpha1.json
apiVersion: kopiur.home-operations.com/v1alpha1
kind: SnapshotPolicy
metadata:
  name: ${KOPIUR_NAME:=${APP}}
spec:
  repository:
    kind: ClusterRepository
    name: ${KOPIUR_REPOSITORY:=garage-raspberrypi}
  credentialProjection:
    enabled: true
  # oliver has no CSI snapshot-controller/VolumeSnapshotClass (local-path only).
  copyMethod: ${KOPIUR_COPYMETHOD:=Direct}
  compression:
    compressor: zstd
  mover:
    inheritSecurityContextFrom:
      pvcConsumer: {}
  sources:
    - pvc:
        name: ${KOPIUR_PVC:=${APP}}
  retention:
    keepDaily: ${KOPIUR_KEEPDAILY:=7}
    keepWeekly: ${KOPIUR_KEEPWEEKLY:=0}
