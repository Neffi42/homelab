# yaml-language-server: $schema=https://raw.githubusercontent.com/bjw-s-labs/helm-charts/main/charts/other/app-template/schemas/helmrelease-helm-v2.schema.json
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: fb-quantum
spec:
  chartRef:
    kind: OCIRepository
    name: fb-quantum
  interval: 1h
  driftDetection:
    mode: enabled
  values:
    controllers:
      main:
        annotations:
          reloader.stakater.com/auto: "true"
        type: statefulset
        containers:
          app:
            image:
              repository: docker.io/gtstef/filebrowser
              tag: stable@sha256:7c5d7ac8ffda31294d278063cf9d2e04303b39e6dce1f4c691342240ca7703b8
            restartPolicy: Always
            env:
              - name: TZ
                value: Europe/Paris
            envFrom:
              - secretRef:
                  name: fb-quantum-credentials
            probes:
              liveness: &probe
                enabled: true
                custom: true
                spec:
                  httpGet:
                    port: &httpPort 80
                    path: /health
                  initialDelaySeconds: 10
                  periodSeconds: 30
                  timeoutSeconds: 3
                  failureThreshold: 3
              readiness: *probe
            resources:
              requests:
                cpu: 100m
              limits:
                memory: 1Gi
    persistence:
      data:
        type: persistentVolumeClaim
        accessMode: ReadWriteOnce
        storageClass: local-path-oliver-ssd
        size: 200Gi
        globalMounts:
          - path: /files
            subPath: files
          - path: /db
            subPath: db
      config:
        type: configMap
        name: fb-quantum
        globalMounts:
          - path: /home/filebrowser/data
            readOnly: true
    service:
      main:
        controller: main
        ports:
          http:
            port: *httpPort
            protocol: TCP
    route:
      http:
        kind: HTTPRoute
        hostnames:
          - "cloud.neffi.fr"
        parentRefs:
          - name: private
            namespace: network
        rules:
          - backendRefs:
              - name: fb-quantum
                port: *httpPort
