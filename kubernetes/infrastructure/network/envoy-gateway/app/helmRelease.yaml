apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: envoy-gateway
spec:
  interval: 10m
  chart:
    spec:
      chart: gateway-helm
      version: "1.7.3"
      sourceRef:
        kind: HelmRepository
        name: envoy-gateway
