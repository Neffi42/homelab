apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: external-secrets
spec:
  interval: 10m
  chart:
    spec:
      chart: external-secrets
      version: "v2.4.1"
      sourceRef:
        kind: HelmRepository
        name: external-secrets
  values:
    bitwarden-sdk-server:
      enabled: true
